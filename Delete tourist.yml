info:
  name: Delete tourist
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{base_url}}/api/tourists/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
