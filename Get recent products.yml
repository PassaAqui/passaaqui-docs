info:
  name: Get recent products
  type: http
  seq: 28

http:
  method: GET
  url: "{{base_url}}/api/products/recent"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
