info:
  name: Get POI by id
  type: http
  seq: 33

http:
  method: GET
  url: "{{base_url}}/api/pois/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
