info:
  name: Get all tourist
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/tourists/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
