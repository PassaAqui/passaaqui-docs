info:
  name: Get all shopkeeper
  type: http
  seq: 12

http:
  method: GET
  url: "{{base_url}}/api/shopkeepers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
