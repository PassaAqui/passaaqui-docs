info:
  name: Get product by id
  type: http
  seq: 27

http:
  method: GET
  url: "{{base_url}}/api/products/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
