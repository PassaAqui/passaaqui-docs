info:
  name: Get tourist by identifier
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/tourists/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
