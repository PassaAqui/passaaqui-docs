info:
  name: Get all POIs
  type: http
  seq: 32

http:
  method: GET
  url: "{{base_url}}/api/pois"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
