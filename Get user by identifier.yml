info:
  name: Get user by identifier
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/users/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
