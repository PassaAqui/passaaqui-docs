info:
  name: Get category by id
  type: http
  seq: 22

http:
  method: GET
  url: "{{base_url}}/api/categories/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
