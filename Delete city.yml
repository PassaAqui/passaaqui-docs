info:
  name: Delete city
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/api/city/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
