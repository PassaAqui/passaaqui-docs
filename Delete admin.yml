info:
  name: Delete admin
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/api/admin/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
