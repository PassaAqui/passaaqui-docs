info:
  name: Update admin
  type: http
  seq: 7

http:
  method: PUT
  url: "{{base_url}}/api/admin/1"
  body:
    type: json
    data: |-
      {
        "email": "admin_atualizado@exemplo.com",
        "name": "Nome Atualizado",
        "password": "novaSenhaSegura@123",
        "adminType": "ADMIN_USER"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
