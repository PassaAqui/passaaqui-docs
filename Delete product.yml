info:
  name: Delete product
  type: http
  seq: 29

http:
  method: DELETE
  url: "{{base_url}}/api/products/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
