info:
  name: Get admin by email
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/admin/email?email=admin@exemplo.com"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
