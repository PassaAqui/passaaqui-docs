info:
  name: Delete category
  type: http
  seq: 24

http:
  method: DELETE
  url: "{{base_url}}/api/categories/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
