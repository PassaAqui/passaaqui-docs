info:
  name: Create city
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/city/create"
  body:
    type: json
    data: |-
      {
        "ibgeCode": "2600000",
        "description": "Recife - PE"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
