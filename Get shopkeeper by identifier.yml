info:
  name: Get shopkeeper by identifier
  type: http
  seq: 14

http:
  method: GET
  url: "{{base_url}}/api/shopkeepers/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
