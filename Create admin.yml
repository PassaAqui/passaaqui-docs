info:
  name: Create admin
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/admin"
  body:
    type: json
    data: |-
      {
        "email": "admin@exemplo.com",
        "name": "Administrador Principal",
        "password": "senhaSegura@123",
        "adminType": "ADMIN_ROOT"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
